# 배포 루트 — 작업공간이 실제로 놓이는 곳
WORKROOM_HOME=/home/portal/workspaces

# 포털 내부 파일(설정·감사로그·캐시)이 쌓이는 곳
# 미지정 시 $WORKROOM_HOME/admin/portal
WORKROOM_PORTAL_HOME=

# 에이전트 실행 바이너리 (PATH 에 있으면 이름만)
HERMES_BIN=hermes

# 비동기 작업 큐 파일 · 로그 디렉터리 (미지정 시 포털 홈 아래)
WORKROOM_AGENT_JOBS_PATH=
WORKROOM_AGENT_JOB_LOG_DIR=
